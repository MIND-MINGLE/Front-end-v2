server {
    listen 5173;  # Change from 80 to 5173
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html; # Handle React SPA routing
    }
}